name: Build and Release with Conda

# Release pipeline: builds every Windows variant and publishes a GitHub Release.
# Triggered only on version tags (and manual dispatch). Plain build-and-check on
# pushes/PRs is handled by ci.yml, which reuses the same build workflow.
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release version (e.g., v1.0.0)'
        required: false
        type: string
      include_chickenrice:
        description: 'Include Chickenrice model in releases'
        required: false
        type: boolean
        default: false

jobs:
  # Build every Windows variant and run the executable smoke (e2e) checks,
  # then upload each build as an artifact for the publish jobs below.
  # Shared with ci.yml (which calls the same workflow with upload_artifacts: false).
  build-windows:
    name: Build Windows
    uses: ./.github/workflows/build-windows.yml
    # upload_artifacts defaults to true, so each variant is uploaded as an
    # artifact that the create-release / upload-* jobs download in this same run.

  # Create the initial GitHub release
  create-release:
    name: Create GitHub Release
    needs: [build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    outputs:
      release_created: ${{ steps.create.outputs.release_created }}

    steps:
    - name: Checkout code for release notes
      uses: actions/checkout@v4
      with:
        sparse-checkout: |
          RELEASE_NOTES_CN.md
        sparse-checkout-cone-mode: false

    - name: Read release body
      id: read_body
      run: |
        if [ -f "RELEASE_NOTES_CN.md" ]; then
          echo 'body<<EOF' >> $GITHUB_OUTPUT
          cat RELEASE_NOTES_CN.md >> $GITHUB_OUTPUT
          echo 'EOF' >> $GITHUB_OUTPUT
        else
          echo 'body=Release created by GitHub Actions' >> $GITHUB_OUTPUT
        fi

    - name: Create empty placeholder file for initial release
      run: |
        echo "This release contains large binary files. Files are being uploaded..." > placeholder.txt

    - name: Create Release with placeholder
      id: create
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        name: ${{ github.ref_name }}
        body: ${{ steps.read_body.outputs.body }}
        draft: false
        prerelease: false
        files: placeholder.txt
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set output
      run: echo "release_created=true" >> $GITHUB_OUTPUT

  # Parallel upload jobs - each handles one artifact
  upload-cu118:
    name: Upload CUDA 11.8 Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_cu118
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for CUDA 11.8 base variant..."
        # Using compression level 5 for faster builds (was level 9)
        # Level 5 provides good balance between speed and compression ratio
        zip -5 -r -q ../faster_whisper_transwithai_windows_cu118.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_cu118.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_cu118.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-cu118-chickenrice:
    name: Upload CUDA 11.8 Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_cu118-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for CUDA 11.8 chickenrice variant..."
        # Using compression level 5 for faster builds (was level 9)
        zip -5 -r -q ../faster_whisper_transwithai_windows_cu118-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_cu118-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_cu118-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-cu122:
    name: Upload CUDA 12.2 Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_cu122
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for CUDA 12.2 base variant..."
        # Using compression level 5 for faster builds (was level 9)
        zip -5 -r -q ../faster_whisper_transwithai_windows_cu122.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_cu122.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_cu122.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-cu122-chickenrice:
    name: Upload CUDA 12.2 Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_cu122-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for CUDA 12.2 chickenrice variant..."
        # Using compression level 5 for faster builds (was level 9)
        zip -5 -r -q ../faster_whisper_transwithai_windows_cu122-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_cu122-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_cu122-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-cu128:
    name: Upload CUDA 12.8 Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_cu128
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for CUDA 12.8 base variant..."
        # Using compression level 5 for faster builds (was level 9)
        zip -5 -r -q ../faster_whisper_transwithai_windows_cu128.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_cu128.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_cu128.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-cu128-chickenrice:
    name: Upload CUDA 12.8 Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_cu128-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for CUDA 12.8 chickenrice variant..."
        # Using compression level 5 for faster builds (was level 9)
        zip -5 -r -q ../faster_whisper_transwithai_windows_cu128-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_cu128-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_cu128-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx101x:
    name: Upload AMD gfx101X Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx101x_dgpu
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx101X base variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx101x_dgpu.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx101x_dgpu.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx101x_dgpu.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx101x-chickenrice:
    name: Upload AMD gfx101X Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx101x_dgpu-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx101X chickenrice variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx101x_dgpu-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx101x_dgpu-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx101x_dgpu-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx103x:
    name: Upload AMD gfx103X Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx103x_dgpu
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx103X base variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx103x_dgpu.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx103x_dgpu.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx103x_dgpu.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx103x-chickenrice:
    name: Upload AMD gfx103X Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx103x_dgpu-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx103X chickenrice variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx103x_dgpu-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx103x_dgpu-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx103x_dgpu-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx110x:
    name: Upload AMD gfx110X Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx110x_all
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx110X base variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx110x_all.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx110x_all.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx110x_all.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx110x-chickenrice:
    name: Upload AMD gfx110X Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx110x_all-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx110X chickenrice variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx110x_all-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx110x_all-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx110x_all-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx120x:
    name: Upload AMD gfx120X Base
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx120x_all
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx120X base variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx120x_all.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx120x_all.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx120x_all.zip
        token: ${{ secrets.GITHUB_TOKEN }}

  upload-amd-gfx120x-chickenrice:
    name: Upload AMD gfx120X Chickenrice
    needs: [create-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v6
      with:
        name: faster_whisper_transwithai_windows_gfx120x_all-chickenrice
        path: artifact/

    - name: Create archive with optimized compression
      run: |
        cd artifact
        echo "Creating archive for AMD gfx120X chickenrice variant..."
        zip -5 -r -q ../faster_whisper_transwithai_windows_gfx120x_all-chickenrice.zip .
        cd ..
        echo "Archive created: $(ls -lh faster_whisper_transwithai_windows_gfx120x_all-chickenrice.zip | awk '{print $5}')"

    - name: Upload to release with large file support
      uses: ading2210/gh-large-releases@v1
      with:
        repository: ${{ github.repository }}
        tag_name: ${{ github.ref }}
        files: faster_whisper_transwithai_windows_gfx120x_all-chickenrice.zip
        token: ${{ secrets.GITHUB_TOKEN }}
